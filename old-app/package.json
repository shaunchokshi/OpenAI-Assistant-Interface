{
  "name": "openai-assistant-interface",
  "version": "1.0.0",
  "description": "OpenAI Assistant API Interface",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "trunk": "trunk",
    "lint": "trunk check",
    "fmt": "trunk fmt"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@trunkio/launcher": "^1.3.4"
  }
}
